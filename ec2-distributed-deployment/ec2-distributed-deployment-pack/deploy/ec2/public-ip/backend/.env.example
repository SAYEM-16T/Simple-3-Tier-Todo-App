# Replace DB_PUBLIC_IP with the DB EC2 public IP
# Replace FRONTEND_PUBLIC_URL with http://<FRONTEND_PUBLIC_IP>
DB_HOST=DB_PUBLIC_IP
DB_PORT=5432
DATABASE_URL=postgresql+psycopg://postgres:StrongPass@123@DB_PUBLIC_IP:5432/todo_db
SECRET_KEY=change-this-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
CORS_ORIGINS=http://FRONTEND_PUBLIC_URL
